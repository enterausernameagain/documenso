legacy-peer-deps = true
prefer-dedupe = true
# Ensure build tooling in devDependencies is installed on platforms that default
# to production-only installs (common in some PaaS environments).
production = false